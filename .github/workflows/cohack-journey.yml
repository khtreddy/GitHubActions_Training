name: CoHackJourney

on: push

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World!"

      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: List Files
        run: ls -la
